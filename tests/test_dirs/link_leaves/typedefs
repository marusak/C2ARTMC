
typedef struct Top
/*? x ?*/
{
  struct Inner * left ;
  struct Inner * right ;
  struct Any * parrent ; /*? null ?*/
} 
+
{
  struct Inner * left ; /*? null ?*/
  struct Inner * right ; /*? null ?*/
  struct Any * parrent ; /*? null ?*/
};


typedef struct Inner
{
  struct Inner * left ;
  struct Inner * right ;
  struct Any * parrent ; /*? -left + -right  ?*/
} 
+
{
  struct Inner * left ; /*? null ?*/
  struct Inner * right ; /*? null ?*/
  struct Any * parrent ; /*? -left + -right ?*/
};




